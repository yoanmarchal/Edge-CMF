---
import AdminShell from '../../layouts/AdminShell.astro';
import Appearance from '../../islands/Appearance';
import { getSessionUser } from '../../lib/session';

const session = await getSessionUser(Astro.cookies);
if (session === null) return Astro.redirect('/');
if (session.user.role !== 'admin') return Astro.redirect('/?error=droits');
---

<AdminShell
  title="Apparence"
  user={session.user}
  description="Thème du site public : le thème de base définit les tokens, les thèmes enfants ne surchargent que ce qu'ils changent."
>
  <Appearance client:only="preact" />
</AdminShell>
