---
import AdminShell from '../../layouts/AdminShell.astro';
import MediaLibrary from '../../islands/MediaLibrary';
import { getSessionUser, canWrite } from '../../lib/session';

const session = await getSessionUser(Astro.cookies);
if (session === null) return Astro.redirect('/');
---

<AdminShell title="Médias — CMF Edge Admin" user={session.user}>
  <MediaLibrary client:only="preact" canWrite={canWrite(session.user)} />
</AdminShell>
